const config = {
  metadata: {
    title: 'DeFi Builder | Solana Hackathon',
    // eslint-disable-next-line quotes
    description: "DeFi Builder's project for Solana Hackathon",
    keywords:
      'next.js, next.js-template, next.js-boilerplate, typescript, shadcn/ui, tailwindcss, eslint, prettier, husky, commitlint, lint-staged'
  },
  server: {
    host: 'localhost',
    port: 3000
  }
};

export default config;
